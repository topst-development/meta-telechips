[Unit]
Description=telechips str manager for suspend & resume
After=multi-user.target

[Service]
Type=forking
PIDFile=/run/str-man.pid
ExecStart=/usr/bin/TCSTRManager -d 1 -t sub

[Install]
WantedBy=multi-user.target

